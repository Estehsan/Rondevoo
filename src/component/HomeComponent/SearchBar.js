import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SearchBar = () => {
  return (
    <View>
      <Text>Search sadsa</Text>
    </View>
  );
};

export default SearchBar;

const styles = StyleSheet.create({});
